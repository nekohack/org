{
  "root": "apps/company-app",
  "sourceRoot": "apps/company-app",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/company-app",
        "outputPath": "dist/apps/company-app"
      },
      "configurations": {
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "options": {
        "buildTarget": "company-app:build",
        "dev": true
      },
      "configurations": {
        "production": {
          "buildTarget": "company-app:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "company-app:build:production"
      }
    },
    "tsm": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "command": "tsm src",
        "cwd": "apps/company-app",
        "parallel": false
      }
    },
    "lint": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "commands": [
          "prettier --write \"**/*.{js,jsx,json,ts,tsx,md,mdx,css,html,yml,yaml,scss,sass}\" --ignore-path .gitignore",
          "eslint ./src --ext .tsx,.ts,.jsx,.js --fix",
          "stylelint --config .stylelintrc.yml 'src/styles/**/*.scss' --fix"
        ],
        "cwd": "apps/company-app",
        "parallel": false
      }
    }
  },
  "tags": []
}
